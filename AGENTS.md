# AGENTS.md (这个是给AI开发看的,当然你也可以看一下)

## 0. 关于作者 / 协作原则

我是 Echoes678，更愿意被当作艺术家而不是程序员。完美主义只用在我在意
的事情上——核心逻辑和主项目零将就；边角/我不感兴趣的模块可以适度放
宽标准，你自行判断，不用每处都死磕。极简对我来说不是风格是秩序感：
几何精度（对齐规整）+ 做减法（砍掉一切不必要的）。我受不了任何"失控
感"——依赖地狱、体积膨胀、卸载残留，本质上都是熵增，能不产生就不产生。

以下是灵活参考，不是铁律，具体怎么判断你自己拿捏：

- "能跑但脏" vs "慢一点但干净可控"——默认选后者。
- 我懒得开局，一旦犹豫要不要动手，直接给个最小可执行起点，别等我把
  全部细节想清楚再问。
- 别用"这已经很好了"这种话敷衍，我要的是经得起自己回头看的东西。
- 汇报/解释不用刻意正式，说重点，不用每次都完整背景铺垫。

---

## 1. 【最高优先级】存疑必全仓库排查

对代码、路径、变量、配置项有任何疑问，下结论、判定 Bug 或修改前，**必
须全仓库检索相关文件**（至少含 `install.sh`、`lib/*.sh`、`v2/**/*.sh`
及其引用文件）。

- 禁止仅凭单文件或"看起来像"的假设下结论。
- 禁止在未完成全局排查前判定某处为硬编码/缺陷并擅自修复。
- 排查方式：全库 `grep` 检索对象的所有引用点，确认是否已有对应处理逻
  辑（如 `sed` 替换、条件判断、环境检测等）。
- 排查后仍无法确定，必须列出已排查文件清单并说明理由。
- 不因改动小或时间紧而跳过。

---

## 2. 架构与目录约定

| 目录 | 职责 |
|---|---|
| `v2/` | Dotfiles 配置源码（niri、noctalia 等的 `.kdl` / 配置模板） |
| `lib/` | Shell 部署 / 诊断 / 备份引擎（`deploy.sh`、`main.sh` 等） |
| `install.sh` | 统一入口点，仅负责环境预检并 `exec` 转入 `lib/main.sh` |

**物理隔离**：仓库源码与 `~/.config/` 隔离，仅允许通过 `lib/deploy.sh`
的 `atomic_replace_item` 机制复制/替换，禁止 `ln -s` 软链接进 `~/.config/`
（`~/.config/` 内部文件之间的软链接，如运行时主题切换，不受此限）。

**Dunder Protocol**：文件名含 `__custom__`（如 `01__custom__.kdl`）或
`~/.config/niri/monitor.kdl` 在更新时会被原子替换引擎识别并保留。

---

## 3. 关键命令

```bash
# 语法与静态检查（改动后必跑）
bash -n install.sh lib/*.sh v2/niri/*.sh v2/noctalia/*.sh
shellcheck install.sh lib/*.sh

# 测试与诊断
HOME=$(mktemp -d) ./install.sh deploy config     # 沙箱隔离测试
./install.sh deploy config < /dev/null           # 实机非交互部署
./install.sh doctor                              # 系统诊断
./install.sh test                                # 实机测试
```

**提交前验证（按改动风险分级，非一刀切）**：

| 改动类型 | 必须做的 |
|---|---|
| 任何改动 | 语法检查 `bash -n` + `shellcheck`（成本极低，无例外） |
| 逻辑/流程改动（非纯文档/注释/Changelog） | 追加沙箱部署测试 |
| 涉及路径替换、GPU 检测、快照/回滚、权限相关逻辑 | 沙箱测试 + 实机验证都不能省 |

不确定改动属于哪一档时，按更高档处理。

---

## 4. 开发铁律速查

| 场景 | 要求 |
|---|---|
| 对代码/路径/变量存疑 | 见第 1 条，必须全仓库排查后再下结论 |
| 部署仓库文件到 `~/.config/` | 只能走 `atomic_replace_item`，禁止软链接 |
| 文件/目录名含 `__custom__` | 更新时自动保留，不需手动处理 |
| 脚本以 `id -u == 0` 运行 | `install.sh`/`nyxniri` 直接拒绝；系统级维护脚本（如 `clean-cache`）例外，允许要求 root |
| `v2/` 模板里的 `/home/user` | 占位符，由 `lib/deploy.sh` 部署时 `sed` 替换为目标 `$HOME`，勿改为硬编码 |
| sed 动态替换路径 | 转义 `\|` 和 `&`（见下方代码块） |
| NVIDIA env 变量 | 默认注释，仅 `lspci` 检测后由 `lib/deploy.sh` 自动解注释，绝不能默认开启 |
| `export VAR=$(mktemp)` | 禁止（见下方代码块） |
| 网络命令（curl 等） | 必须带 `--connect-timeout`，非关键调用加 `\|\| true` 容错 |
| 依赖非 0 退出码做流程控制的脚本 | 用 `set -uo pipefail`，不用 `set -e` |
| 引擎代码（`lib/*.sh`） | 避免硬编码特定项目名，用 `$PROJECT_NAME`/`$CLI_CMD`/`$MAIN_WM`/`$THEME_ENGINE` 等全局变量；TUI 文案可适当灵活 |

**sed 转义**：
```bash
sed "s|PLACEHOLDER|$(printf '%s' "$target_path" | sed 's/[&|]/\\&/g')|g"
```

**mktemp 陷阱**：`export VAR=$(mktemp)` 会吞掉失败返回值，必须拆两行：
```bash
VAR=$(mktemp) || { echo "mktemp failed" >&2; exit 1; }
export VAR
```

---

## 5. Changelog 规范（严格执行，无例外）

每条记录必须同时满足以下三条，缺一不可：

1. **极简**：单条改动一行以内，禁止罗列内部实现细节/函数名/文件路径。
2. **通俗**：面向普通用户的自然语言。禁止工程黑话（"重构""解耦""状态
   机"等），禁止 AI 套话（如"提升了整体体验""进一步优化了性能"这类
   空洞表述）。
3. **真实 / 净变更**：只记录相对上一个**已发布**版本的净变更。开发过
   程中引入、且在该版本发布前就已修复的 bug，**不写**——这是开发中间
   状态，不是对用户而言真实存在过的问题。

写完后自查：如果删掉这条记录用户会不会真的错过重要信息？如果不会，
删掉，不为了显得"改动很多"而凑数。

---

## 6. 安装交互与用户体验

- **非交互式优先原则 (Non-interactive/Express Mode)**：当执行“一键部署”或自动化安装流程（例如核心安装逻辑使用 `express` 参数时），应当尽可能避免在安装循环中弹出阻断式的交互确认（如繁杂的 `Y/n` 选择），保持流程的流畅性。
- **部署前确认 (Pre-flight Checklist)**：为了提升用户体验，建议在执行真正的安装与文件覆盖逻辑之前，统一进行前置检查（如调用 `_phase_preflight_check` ）。在此阶段集中向用户展示“即将变更的操作清单”，并视情况提前通过 `sudo -v` 索取权限，以确保随后的自动化流程不再被打断。

---

## 7. 宏观 TUI 设计宪章 (Macro TUI Design Charter)

- **秩序与几何 (Order & Monospace Geometry)**：所有终端输出均视为等宽字符网格上的空间排版。遵守严格的列对齐与几何边界，绝不容许字符错位或边界漂移。中英文混排或双列 Unicode 字符必须经过显示宽度换算，保持视觉边缘的绝对规整。
- **减法与降噪 (Reductionism & Anti-Noise)**：剥离无意义的彩色图标与 Emoji 堆砌，仅保留必要的结构分割与状态语义表达，呈现高质感、纯粹的终端质感。以行间距与层级缩进作为第一视线引导元素，拒绝密密麻麻的文本堆叠。
- **控制感与无熵 (Silent Execution & Low Entropy)**：过程状态就地收拢或静默，避免无意义的滚屏刷屏破坏终端上下文历史。常规操作轻量流畅；写磁盘/覆盖配置等破坏性操作必须具备清晰的前置变更清单与显式防误触确认。

基于你的 `AGENTS.md` 和你对“秩序感、做减法、零熵增”的极致追求，我为你制定了以下文字规范宪章。这份宪章摒弃了一切冗余的修饰，吸收了苹果文案的“冷静、客观、精准”特质，专注于软件界面与内部文案的规范化。

***

## 文案规范宪章

### 0. 核心理念
- **极简即秩序**：文字如代码，拒绝冗余。每个字必须有其存在的理由，若无理由，一律砍掉。
- **零认知摩擦**：直陈事实与动作，不修饰、不绕弯、不情绪化。
- **克制与客观**：追求苹果式的冷静质感——说清楚“是什么”和“做什么”，绝不吹嘘“为什么好”。

### 1. 软件界面文案规范
适用于按钮、菜单、状态提示、错误警告等交互文本。

- **动词驱动**：动作指令必须是纯粹的祈使句。
  - ✅ `覆盖配置` / `中止` / `重试`
  - ❌ `点击这里覆盖` / `你是否要中止？`
- **名词收敛**：标签与标题使用极简名词，剥离修饰性后缀。
  - ✅ `诊断` / `路径设置`
  - ❌ `系统诊断报告工具` / `路径设置配置项`
- **状态静默**：过程提示就地收拢，不使用进行时的冗长描述。
  - ✅ `部署中…` / `正在校验`
  - ❌ `正在为您努力部署相关文件，请稍候…`
- **错误直白**：错误提示遵循「事实 + 原因 + 下一步」结构，不带歉意。
  - ✅ `权限不足。请使用 sudo 重试。`
  - ❌ `哎呀，出错了！似乎你没有权限执行此操作。`
- **防呆确认**：破坏性操作前给清单，不用疑问句试探。
  - ✅ `将覆盖 3 个文件。继续？`
  - ❌ `确定要执行覆盖操作吗？`

### 2. 介绍与文档文案规范
适用于 README、功能介绍等长文本。

- **直击本质**：第一句话说明模块是什么、解决什么问题。不写时代背景。
  - ✅ `Niri 配置部署器，负责从源码原子替换至 ~/.config/。`
  - ❌ `在当今复杂的桌面环境中，一个优秀的配置部署器是必不可少的…`
- **短句优先**：长句拆短。主谓宾结构清晰，少用从句和连词。

### 3. 词汇禁用清单
为彻底去除“AI味”与“工程黑话”，以下词汇在面向用户的文案中**绝对禁用**：

- **空洞修饰类**：体验、无缝、赋能、智能化、进一步、更好地、全面、强大。
- **AI 谄媚类**：您、呢、哦、哎呀、抱歉、让我们。
- **伪专业类**：优化了性能、提升了稳定性（必须给出具体数值或直接删除，如“快 20%”）。

## 4. 标点与排版
- **中英文混排**：中文与英文/数字之间强制加半角空格（如 `部署 3 个文件`）。
- **标点极简**：能用句号不用感叹号。UI 文案尽量不带句号，列表和短句末尾省略标点。
- **拒绝图标泛滥**：文字本身即是表达，除非具有明确的状态语义（如 `[✓]` 表示成功），否则不插入任何 Emoji 或装饰性图标。

***
*本宪章为强制规范。任何文案在产出前，先做减法，删到不能再删，即为终稿。*
